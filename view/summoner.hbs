<!DOCTYPE html>
<html lang="kr">
<head>
  <meta charset="UTF-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0/dist/tailwind.min.css" rel="stylesheet">
  <title>소환사 프로필</title>
  <style>
    .custom-gradient-bg {
      background-image: linear-gradient(to right, #0a0a0a, #2c3e50);
    }
    .custom-gradient-text {
      background-image: linear-gradient(to right, #f6d365, #fda085);
      -webkit-background-clip: text;
      color: transparent;
    }
    .custom-golden {
      color: #ffd700;
    }
    .custom-golden-border {
      border-color: #ffd700;
    }
    .rainbow-text {
      background-image: linear-gradient(to left, red, orangered, yellow, skyblue, hotpink, greenyellow);
      color: transparent;
      background-clip: text;
      -webkit-background-clip: text; /* Chrome과 Safari를 위한 벤더 프리픽스 */

    }
  </style>
</head>
<body class="custom-gradient-bg min-h-screen flex flex-col text-white">

<header class="text-center py-6">
  <a href="/lolp.gg" class="text-4xl font-bold custom-gradient-text">LOLP.GG</a>
</header>

<main class="max-w-4xl mx-auto p-4">
  <section class="bg-gray-750 rounded-lg p-6 mb-6 transform transition duration-500 hover:scale-105">
    <h2 class="text-3xl font-bold mb-6 text-center rainbow-text">소환사 정보</h2>
    <div class="flex flex-col items-center justify-center space-y-4">
      <img class="w-40 h-40 rounded-full custom-golden-border border-4" src="http://ddragon.leagueoflegends.com/cdn/14.5.1/img/profileicon/{{summoner.profileIconId}}.png" alt="소환사 아이콘">
      <div class="text-center">
        <p class="text-xl mb-1 font-bold custom-golden">{{ summoner.name }}</p>
        <p class="text-lg">레벨 : <span class="font-semibold text-pink-500">{{ summoner.summonerLevel }}</span></p>
        <p class="text-lg">Rank : <span class="font-semibold text-green-500">{{ summonerRank.0.tier }} {{ summonerRank.0.rank }}</span></p>
        <p class="text-lg">LP : <span class="font-semibold text-purple-500">{{ summonerRank.0.leaguePoints }}</span></p>
        <p class="text-lg">승리 : <span class="font-semibold text-blue-500">{{ summonerRank.0.wins }}</span>, 패배: <span class="font-semibold text-red-500">{{ summonerRank.0.losses }}</span></p>
      </div>
    </div>
  </section>

  <section class="bg-gray-750 rounded-lg shadow-lg p-6 mb-6">
    <h2 class="text-3xl font-bold mb-6 text-center text-yellow-300">챔피언 숙련도</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
      {{#each masteryWithNames}}
        <div class="bg-gray-750 rounded-lg p-4 flex flex-col items-center space-y-3 hover:bg-gray-600 transition duration-300 ease-in-out">
          <img
            alt="{{this.championName}}"
            class="w-24 h-24 mb-2 rounded-full"
            src="http://ddragon.leagueoflegends.com/cdn/14.5.1/img/champion/{{this.englishName}}.png">
          <div class="text-center">
            <p class="text-lg font-semibold text-yellow-200">{{this.championName}}</p>
            <p class="text-sm text-yellow-100">숙련도: {{this.championPoints}}</p>
            <p class="text-xs text-yellow-100">마지막 게임: {{this.lastPlayedDate}}</p>
          </div>
        </div>
      {{/each}}
    </div>
  </section>


</main>
</body>
</html>
