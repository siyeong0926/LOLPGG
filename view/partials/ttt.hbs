<section class="bg-gradient-to-r from-blue-900 to-purple-700 rounded-lg shadow-xl p-6 mb-6 transform transition duration-500 hover:scale-105">
  <h2 class="text-2xl font-bold mb-6 text-center text-white">소환사 정보</h2>
  <div class="flex flex-col items-center justify-center space-y-4">
    <img class="w-32 h-32 rounded-full border-4 border-blue-300 shadow-lg" src="http://ddragon.leagueoflegends.com/cdn/14.5.1/img/profileicon/{{summoner.profileIconId}}.png" alt="소환사 아이콘">
    <div class="text-white">
      <p class="text-xl mb-1">소환사명: <span class="font-semibold">{{ summoner.name }}</span></p>
      <p class="text-lg">레벨: <span class="font-semibold">{{ summoner.summonerLevel }}</span></p>
      <p class="text-lg">Rank: <span class="font-semibold">{{ summonerRank.0.tier }}</span></p>
      <p class="text-lg">LP: <span class="font-semibold">{{ summonerRank.0.leaguePoints }}</span></p>
      <p class="text-lg">WIN: <span class="font-semibold">{{ summonerRank.0.wins }}</span> LOSSES: <span class="font-semibold">{{ summonerRank.0.losses }}</span></p>
    </div>
  </div>
</section>